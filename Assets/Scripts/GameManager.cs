using UnityEngine;

public class GameManager : MonoBehaviour
{
    // Al ser 'static', cualquier script puede sumar puntos fácilmente
    public static int enemigosEliminados = 0;

    // Solo para verlo en la consola de momento
    void Update()
    {
        // Puedes borrar este Update más adelante, es solo para comprobar que funciona
    }
}